

name: CICD


on:
  push:
    branches: 
      - "main" 
    
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:

  build:

    runs-on: ubuntu-latest


    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Node setup
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Instalar dependencias
        run: npm ci

      - name: Mensagem
        run: echo "evento:{{ github.event_name }}"
          - echo "servidor:${{ runner.os }}"
          - echo "repositório:${{ github.repository }}"

